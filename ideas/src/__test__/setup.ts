// import { clearDb, closeDb, makeDb } from '../__mock__/mock-db'

beforeAll(async () => {
  // await makeDb()
})

beforeEach(async () => {
  // await clearDb()
})

afterAll(async () => {
  // await closeDb()
})
